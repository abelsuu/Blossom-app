import 'package:flutter/material.dart';

Widget buildMapEmbed(double lat, double lng) {
  return Container(
    color: const Color(0xFFe5e3df),
    alignment: Alignment.center,
    child: const Icon(Icons.map_outlined, size: 64, color: Colors.blue),
  );
}

